// import * as cheerio from "cheerio";

export const parseMetaTags = (html: string) => {
  // const $ = cheerio.load(html);

  // const title = $("title").text().trim();
  // const titleLength = title.length;

  // const description = $('meta[name="description"]').attr("content") || "";
  // const descriptionLength = description.length;

  // const keywords = $('meta[name="keywords"]').attr("content") || null;

  // const viewport = $('meta[name="viewport"]').attr("content") || null;

  // const ogTags = {
  //   "og:title": $('meta[property="og:title"]').attr("content") || null,
  //   "og:description": $('meta[property="og:description"]').attr("content") || null,
  //   "og:image": $('meta[property="og:image"]').attr("content") || null,
  //   "og:url": $('meta[property="og:url"]').attr("content") || null,
  // };

  // return {
  //   title,
  //   titleLength,
  //   description,
  //   descriptionLength,
  //   keywords,
  //   viewport,
  //   ogTags,
  // };
};
